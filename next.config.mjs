/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['your-image-domain.com'], // Add any external domains you're loading images from
  },
};

export default nextConfig;